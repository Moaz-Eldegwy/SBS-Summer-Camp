# Roster contains 180 real names, IDs and emails — it seeds the Google Sheet
# and must never be published to GitHub Pages.
roster/

# Local Gemini API key for the live image-generation slide — never commit.
gemini-key.js
